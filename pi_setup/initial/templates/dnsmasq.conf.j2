interface=wlan0 # Listening interface
dhcp-range={{ dhcp_range_start }},{{ dhcp_range_end }},255.255.255.0,24h # Pool of IP addresses served via DHCP
domain=wlan     # Local wireless DNS domain
address=/{{router_dns_name}}/{{ router_static_ip}} # Alias for this router